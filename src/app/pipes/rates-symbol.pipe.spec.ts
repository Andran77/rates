import { RatesSymbolPipe } from './rates-symbol.pipe';

describe('RatesSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new RatesSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
